const Loading = () => {
    return (
        // instalar bootstrap para aparecer o loading
        <div className="spinner-border text-secondary" role="status">
            <span className="sr-only">Carregando...</span>
        </div>
    )
}

export default Loading;